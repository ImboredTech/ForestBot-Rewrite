steps:
- name: 'gcr.io/cloud-builders/gcloud'
  ip: Update staging server
  entrypoint: /bin/bash
  args:
  - '-c'
  - |
    set -x && \
    gcloud compute ssh instance-1 --zone=europe-north1-a --command='/bin/bash /home/bobdylangsrp/update.sh'